export const metadata = {
  title: "User Login - CoPassage",
  description: "Sign in to CoPassage to find rides or offer empty seats",
};

export default function UserLoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
