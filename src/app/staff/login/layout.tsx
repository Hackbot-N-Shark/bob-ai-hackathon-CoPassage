export const metadata = {
  title: "City Staff Login - CoPassage",
  description: "Secure access for CoPassage City Staff",
};

export default function StaffLoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
